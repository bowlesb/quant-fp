[package]
name = "quant_tick"
version = "0.1.0"
edition = "2021"

[lib]
name = "quant_tick"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py312"] }

[profile.release]
opt-level = 3
lto = true
