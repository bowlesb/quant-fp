# Web layer
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Trust DB + feature-store / registry introspection (quantlib.features import chain).
# quant_tick (the compiled Rust kernel two feature groups import) is installed from a wheel in the
# Dockerfile's build stage, not from PyPI.
psycopg[binary]==3.2.3
polars
numpy
# The always-warm store-grid cache lives in a dedicated MongoDB service: the worker writes the precomputed
# 18-month coverage grid (+ drills + meta) there and the dashboard reads it, so a refresh is one indexed doc
# fetch off the request path. pymongo is the client for both the worker and the dashboard read routes.
pymongo>=4.6,<5
# The curated feature-group guide (docs/feature_group_guide.yaml) the detail panel reads is parsed at worker
# build time via PyYAML — the only new dep for the group detail panel.
pyyaml>=6,<7
# The hourly status dashboard's append-only JSON store is shared between the HOST Lead loop and the container's
# reaction POST; filelock serializes the read-modify-write so neither writer clobbers the other's append.
filelock>=3.13,<4

