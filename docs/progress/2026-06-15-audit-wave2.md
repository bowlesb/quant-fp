# Data-Quality Audit — Wave 2 (8 groups) — 2026-06-15

8 read-only auditors (one per group): efficiency, momentum_consistency, clean_momentum, sector,
market_beta, breadth, round_levels, trend_quality. Findings deduped against wave-1 systemic roots.

## OWNER-RELEVANT (top)
- **One LOCAL fix landed this cycle (parity-safe):** `trend_quality` flat-price R² guard — a flat
  window left R² undefined (null on backfill / NaN on live), which nulled `trend_strength` even though
  the slope was a valid ~0. Now pinned to R²=0 (zero *explained* variance) in the shared `assemble()`,
  handling null↔NaN identically so live==backfill by construction. `tests/test_fp_latest.py` +
  `tests/test_fp_stateful_emit.py` green (44 passed); ruff clean.
- **Two NEW systemic CRITICAL/HIGH found, escalated to backlog P1.0 (NOT my lane):**
  1. **breadth computed PER-SHARD** — `REDUCE_GROUPS` (sharded_capture.py:40) omits `breadth`, so it
     runs inside each of 8 shards over ~1/8 of the universe. Proven: 8 distinct breadth values per
     minute, partitioning by `hash(symbol)%8`. All 30 breadth cols wrong + live≠backfill. → CRITICAL-3.
  2. **sector_map table EMPTY** — FMP `/profile` ingestion was never wired; every symbol → `unknown`
     (AAPL/JPM/XOM all unknown). Kills `sector` group AND `breadth.sector_breadth_*`. → HIGH-SECTOR.
- **CRITICAL-1 (points/shift) CONFIRMED** for efficiency (18 feats 100% NaN) and momentum_consistency
  (consistent_direction_* 100% NaN). CRITICAL-2 (restart long-window collapse) CONFIRMED quantitatively
  in trend_quality (`price_r2_90m==120m==180m` for 100% of rows post-restart).

## Verdicts
| Group | Verdict | Notes |
|---|---|---|
| efficiency | blocked-systemic | CRITICAL-1: all 18 feats 100% NaN live; **no backfill exists** ⇒ no parity ref yet |
| momentum_consistency | blocked-systemic | CRITICAL-1: consistent_direction_* dead; accel valid_range slightly tight (max 74) |
| trend_quality | **fixed-code** | flat-price R²→0 guard landed; residual CRITICAL-2 collapse |
| clean_momentum | **clean** (value-side) | all in [0,1], 0 Inf; residuals are systemic (warmup gating + ETF universe) |
| sector | blocked-systemic | HIGH-SECTOR: 100% unknown; code correct, needs FMP ingestion (no recollect) |
| market_beta | blocked-systemic | MED-BETA: ±7700 from 2-pt OLS; SPY self-beta=1.0 (join healthy) |
| breadth | blocked-systemic | CRITICAL-3 per-shard + sector_breadth dead + ETF pollution |
| round_levels | **clean** (math) | 0 NaN/Inf, ranges hold; fixed-$ grid is price-scale-incomparable (modeller note) |

## Cross-cutting (recurring, lower priority)
- **Universe pollution** — warrants (`.WS`, `…W`), preferreds (`.PR`), units (`.U`), leveraged ETFs
  (TQQQ/SQQQ/SOXL) flow into the STREAM feature-input universe across round_levels, market_beta,
  clean_momentum, breadth. The 2026-06-11 ETF lesson recurring. Needs a PIT single-name-equity pin on
  the stream feature path (distinct from the bars subscription). Logged as MED-UNIVERSE.
- **Parity gate blind to one-sided null** (flagged by the momentum_consistency auditor): the bad-row
  filter in `test_fp_latest.py` evaluates to null when exactly one side is null and polars drops it, so
  a live-returns-null vs backfill-returns-real divergence is invisible. Recommend an explicit
  `(exp.is_null() != act.is_null())` FAIL clause — owned by the parity/engine lane.
- **nan_policy="warmup" is metadata, not enforced** — clean_momentum/trend_quality long windows emit
  scores from 1-2 points pre-warmup. Same root as CRITICAL-2's collapse signature; a per-window
  min-count gate is the durable fix (platform/modeller lane).

## Next
Wave 3: remaining UNAUDITED — asset_flags, calendar, calendar_events, cross_sectional_rank,
market_context, momentum_run, multi_day_returns, multi_day_vwap, price_levels, prior_day,
residual_analysis, swing. Still pending: technical long-SMA warmup (ISSUES-LOCAL, local fix).
SEQUENCING RULE still binds — batch all feature-code fixes for one clean restart after CRITICAL-2.
