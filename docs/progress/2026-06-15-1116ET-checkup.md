# Collection checkup — 2026-06-15 11:16 ET (Mon)

**Status: HEALTHY collection at the widened 11.3k universe; one expected FAIL that is an architectural finding, not a regression. Owner is awake and in the loop.**

## Key numbers
- Capture Up, real SIP, 11,336 symbols subscribed, no errors/disconnects.
- Freshness 2.0 min (recovered from two intentional restarts this hour); newest file written <1s ago — pipeline keeping up at 3.7× load.
- Coverage 8,075 / 11,336 (71%) distinct symbols today; per-minute active 4,167 (in-band); A-E share 36.9% (bias gone, was 97%).
- nan_rates sane (worst volume_zscore_3m 14%, warmup-driven); 0 constant features; disk 71% free.
- group_compute p99 = 0.72s (feature math is fast).

## The latency FAIL is a real finding (not a bug)
`bar_to_vector_latency` FAILs at ~60s p50 — and that is the honest, now-measured truth about the **bars-only batch path**:
- A minute's vector cannot be assembled until the minute closes (~T+60s); the reader holds the batch until late-delivered bars stop arriving.
- Drill-down (`latency_slow_symbols`) confirms the slowest names are thin ETFs whose bars Alpaca delivers ~60s late.
- **Pure feature compute is fast (group_compute p99 0.72s).** The ~60s is the minute-close wait + Alpaca delivery, inherent to the batch architecture.
- **Sub-second bet latency is architecturally impossible in this path** — it requires the per-symbol fast/tick path (the long-standing known follow-up). The instrumentation now *proves and sizes* this (~60s floor).

This is exactly what the new latency observability was built to reveal. Grafana dashboard `bar→vector latency` (port 3001) shows end2end vs assemble p50/p95/p99 + per-shard + the slow-ticker drill-down table.

## Remaining WARNs (known register)
- `worker_targets_up`: 8/8 shards UP; 4 stale `ingestor:9101-9104` targets still showing DOWN — the prometheus.yml cleanup is merged but Prometheus hasn't reloaded the scrape config (needs a prometheus restart/reload).
- `validation_freshness` + `trust_grades`: ledger hasn't run yet (empty) — parity validation is the next workstream.

## Shipped this session (all merged to integration/converged, live)
Universe 3,070→11,336 (bias removed); dual latency metric + per-ticker drill-down + Grafana; continuous healthcheck engine + 5-min host-cron tripwire + MAINTENANCE_PROTOCOL.md.

## For the owner
Nothing broken. The "FAIL" is the batch-path latency floor made visible — decide whether to prioritize the per-symbol fast-path next (the only way to sub-minute bets). Also pending: a prometheus reload to clear the stale targets, and standing up the parity validation ledger.
