# Data-Quality Audit — Wave 3 (8 groups) — 2026-06-15 13:13 ET

8 read-only auditors, one per UNAUDITED group, against today's live stream store. Landed one in-lane code
fix (parity-tested + merged); recorded two long-window systemic confirmations and one queued Rust fix.

## OWNER-RELEVANT (top)
- **NEW LOCAL BUG FIXED + MERGED — `momentum_run.residual_skew`** blew up to **±1.6e9 vs declared ±20**
  (0.5% of 5m rows, 184 symbols) from floating-point cancellation noise on near-linear price paths. Fixed
  with a **relative residual-spread floor** (residual std must exceed 1e-6 of price), regression-tested,
  parity green, merged to `integration/converged` (`84bbb7d`). Recompute residual_skew on the clean restart.
- **CRITICAL-2 is wider than "restart"** — wave-3 gives hard evidence it also fires WITHOUT a restart: the
  300m trailing buffer (`capture.py:45`) is shorter than the 390m session, so stateful/cumulative groups
  lose history. Proof: swing `n_pivots_today` **decreases intraday** (TSLA 7→6→0) and `minutes_since_pivot`
  pins at exactly 299; price_levels `dist_from_high_120m==240m` for 100% of late-session rows. The warm-start
  fix must seed/anchor the buffer to **≥ the longest declared window** (240m), not 300m flat.
- **QUEUED Rust fix (my lane, batched)** — `swing.fib_retracement` ranges to **3101 vs declared ±10**
  (`rust/src/lib.rs:860-861`, loose denominator guard). Fix = clamp output to the declared range; it needs a
  `make dev-image` rebuild, so it's batched with the clean restart (swing already needs recollect for CRITICAL-2).

## Verdicts
| group | verdict | one-liner |
|---|---|---|
| asset_flags | **OK** | 4 static flags {0,1}, 0 nulls, correct; only value-safe MED-DEDUP |
| calendar | **OK** | ET-clock EXACT (0/645k mismatch), parity by construction; dtype declared-vs-stored note |
| calendar_events | **OK** | pure timestamp math, opex/quarter-end correct for today; no dead upstream |
| market_context | **OK** | SPY/QQQ broadcast exact, 1 value/min (no per-shard bug); 120m null until buffer warms (coverage) |
| momentum_run | **FIXED-CODE** | residual_skew floor landed+tested; longest_streak clean; immune to CRITICAL-2 |
| price_levels | **BLOCKED-ON-SYSTEMIC** | CRITICAL-2 120m/240m collapse post-open re-seed; algebra correct |
| prior_day | **BLOCKED-ON-SYSTEMIC** | CRITICAL-2-class warm-start: anchor NaN until ~10:58 ET for 26% of names; math clean |
| swing | **BLOCKED-ON-SYSTEMIC** | CRITICAL-2 buffer<session collapses zigzag; + queued local fib clamp |

## Tally after wave 3
4 newly OK (asset_flags, calendar, calendar_events, market_context) + 1 FIXED-CODE (momentum_run).
3 newly BLOCKED-ON-SYSTEMIC on CRITICAL-2 (price_levels, prior_day, swing). All group **code** is correct;
the blockers are the shared buffer/warm-start issue (CRITICAL-2, main-loop-owned) — no per-group bug to fix.

## Next
Remaining UNAUDITED (4): cross_sectional_rank, multi_day_returns, multi_day_vwap, residual_analysis.
PENDING-STREAM (5) await trades/quotes. After the warm-start (CRITICAL-2) lands: one clean restart +
recompute the batched FIXED-CODE groups (volume, trend_quality, momentum_run, return_dynamics, …) and the
CRITICAL-2 long-window recollects (price_levels 120m/240m, swing, prior_day pre-10:58).
