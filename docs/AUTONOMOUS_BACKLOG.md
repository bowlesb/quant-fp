# Autonomous Backlog — the loop's goal-driven queue

The autonomous loop reads THIS file every cycle, maintains the live system, then advances the
single highest-priority unchecked improvement item. It is the operational expression of our goals
(parity-true real-time features × ~10k equities, sub-minute bet latency, validated trust, edge hunt).

**Loop discipline each cycle:** MAINTAIN (healthcheck + safe-fix) → ADVANCE (top unchecked item) →
RECORD (commit + progress note) → REFILL (if the improvement queue is thin, append new high-value
items so the loop NEVER idles). Mark items `[x]` when done, with the commit/branch in the line.

---

## P0 — Maintenance (every cycle, recurring)
- [ ] Run `docker exec feature-computer python -m quantlib.ops.healthcheck`; safe-fix dead/wedged containers and empty universe only; escalate anything else at the TOP of the note.
- [ ] Confirm collection: capture Up, SIP ESTABLISHED, freshness < 3 min, coverage growing, no crash loops.

## P1.0 — DATA QUALITY & PARITY (do FIRST — corrupt/missing feature data invalidates all downstream modeling)
Source: wave-1 data-quality audit, 2026-06-15 (10 groups). Findings are HEAVILY duplicative — fix the systemic root causes once. Full evidence: `docs/progress/2026-06-15-audit-wave1.md`. Deleting/recollecting today's data is explicitly authorized (pre-alpha).
- [x] **CRITICAL — `points()` lag features emit 100% NaN on the live stream (parity break).** ROOT CAUSE WAS BROADER than the incremental engine: **all** assemble paths (`compute_reduction_batch`→`assemble_from_long` — the DEPLOYED default — plus `compute_latest`, `emit_rust`, `emit_rust_unified`, `emit_numpy`, and the incremental `step*`) re-evaluated `points()` exprs on `frame.filter(minute==latest)`, a single-minute frame, so `close.shift(k>0).over("symbol")` → null → 100% NaN for `efficiency_ratio_*`/`directional_efficiency_*` (efficiency, 18), `ret_accel_*` (return_dynamics), `consistent_direction_*` (momentum_consistency). Backfill `compute()` evaluates points over the whole buffer ⇒ live≠backfill. **FIX (commit on `fix/points-lag-live-parity`):** new central `declarative.resolve_points(groups, frame, latest)` resolves point exprs over the WHOLE buffer (gap-safe; positional shift matches backfill cell-for-cell) and returns the latest-minute frame carrying precomputed `__pt_<name>` cols; every assemble path now SELECTs those by name instead of re-evaluating. ALSO fixed a parity-test blind spot: `test_fp_latest.py` masked null-vs-value divergence (the `(a-null)` predicate is null → `~null` → `filter` drops it), so the bug was invisible; the check now flags `is_null() != is_null()`. New direct regression + gap-safety tests added (`test_lag_point_group_is_not_all_null_live`, `test_lag_points_gap_safe_matches_backfill`). 38 pass in test_fp_latest; incremental/unified/stateful/parity/declarative/latency all green. NOTE: this is a feature-VALUE change → deploy via the batched clean restart (SEQUENCING RULE), not a standalone redeploy; then recompute today's contaminated lag-point data from backfill. (Also fixes `trade_flow` points once trades stream.)
- [~] **CRITICAL — capture restart wipes the live ring/incremental buffers → long-window features (60m/120m...) collapse + emit post-warmup NaN for the rest of the session.** Confirmed in volatility/momentum/ohlc_vol; the two resets today (15:00 & 15:11 UTC) were deploy restarts. No warm-start from the store. REQUIRED for the nightly relaunch (#3) too.
  - **2026-06-15 increment (commit `dc0187e` / merge `ceb43e8`, `feat/warm-start-ring` → integration/converged):** built `capture.warm_start_ring(state, bars, depth, project_columns)` — rehydrates `state.ring` from the session's already-settled bars BEFORE the first live minute. Source = `backfill_bars(day, symbols)` (Alpaca historical **RAW** = the same SIP tape the stream delivers), so the seed is **parity-true**: the warmed ring holds exactly the rows the live path would have accumulated. Wired into BOTH launch paths behind **`FP_WARM_START` (default OFF)** — `run_capture` (single), each sharded worker (its shard's map ring), and the reader (the universe-wide reduce ring, projected + depth-capped as `process_reduce` keeps it live). With the flag unset the launch is byte-identical to today's cold start (zero deploy risk). Tests `tests/test_fp_warm_start.py` (6 pass incl. THE parity gate: warm-start through T-1 + process live minute T == cold capture that streamed every minute 0..T); 20 pass across warm_start + sharding + incremental_capture.
  - **RESOLVED (2026-06-15, commit `e2a98c2` / merge `9d479da`):** the buffer-depth question. MEASURED at session depth (`profile 1250 720 --latest`): the session-cumulative groups need the buffer to reach the first collected bar of the UTC day. swing `n_pivots_today`/`minutes_since_pivot` reset at the **UTC day** (rust kernel `minute//86400`, not RTH open), and collection starts premarket **~08:00 UTC (4:00 ET)**, so at the 16:00 ET close the buffer must reach **~720m** back — **the originally-floated 410m is INSUFFICIENT** (key finding). (NB: prior_day anchor + price_levels `dist_from_high` are NOT buffer-depth — those are HIGH-DAILYLOAD and the restart-wipe/warm-start facets respectively; swing is the genuine session-cumulative group.) **Decision: `DEFAULT_BUFFER_MINUTES` 300→750** (720m session + 30m slack). Cost: per-group-summed live recompute ~15.6s/shard at 1250 tickers vs the 60s minute budget — comfortable; deployed BATCHED path is lower. The reduce (universe) ring is capped independently (`sharded_capture.reduce_buffer_minutes`), so only the per-shard MAP ring (where swing lives) deepens. Parity-neutral for all ≤240m-window groups, parity-improving for swing. Ships on the clean restart (NOT restarted here). **Follow-up queued below (P1 refill):** the global depth taxes windowed groups that don't need it (momentum_run needs 60m, dominates the cost) — eliminate via per-group buffer-depth slicing or a stateful-swing accumulator.
  - **NEXT (close-out):** (1) ~~resolve the buffer-depth question~~ DONE; (2) flip `FP_WARM_START=1` at the ONE clean restart (batched with CRITICAL-1 + other FIXED-CODE feature fixes per the SEQUENCING RULE), verify the ring warm-starts (worker logs "warm-started ring: N minutes") + freshness recovers < 3 min + 8/8 shards; (3) **delete & recompute today's contaminated long-window + lag-point data via the backfill `compute()` path** (immune to the reset).
- [ ] **MED — duplicate (symbol,minute) rows (up to 8×, every group).** Index/broadcast symbols (SPY/QQQ/IWM + ETFs) are written by all 8 shards; byte-identical. Benign IF consumers dedup, but fix at the write/compaction layer (single-writer for broadcast symbols, or dedup-on-write keep-last). Until then, all readers MUST `.unique(subset=["symbol","minute"])`.
- [x] **std=0 guards** — `volume_zscore_*` (0/0 NaN) and `bb_position_20m` (±inf) now guarded; parity-safe. (e0c9957)
- [x] **CRITICAL — `breadth` is computed PER-SHARD, not market-wide → every breadth value is wrong + live≠backfill.** `REDUCE_GROUPS` in `sharded_capture.py:40` lists only `cross_sectional_rank`, so `breadth` (a whole-universe GATHER reduce, per its own docstring) runs inside each of the 8 shard workers over only ~1/8 of the universe. Evidence (wave-2): for any fixed minute the "market-wide" breadth scalars take **exactly 8 distinct values** (e.g. `breadth_up_5m` 0.326–0.409 at 16:18 UTC), symbols partitioning by `hash(symbol)%8`. All 30 `breadth_*`/`sector_breadth_*` columns corrupted; backfill is single-process ⇒ parity break. **FIXED-CODE (commit `7f9a357` / merge to integration/converged, `fix/breadth-reduce-whole-universe`):** added `"breadth"` to `REDUCE_GROUPS` (workers now exclude it; reader's gather computes it once over all symbols); threaded the reader's full un-sharded `snapshots` (reference+daily) through `process_reduce`→`process_bars` so breadth self-selects (`runnable`) and computes its sector + 1d/5d horizons over the whole market; added `breadth.reduce_buffer_minutes()`=`max(MINUTE_WINDOWS)`=60 so the reader's minimal reduce ring reaches the deepest intraday horizon (1d/5d read the daily snapshot, not the ring). Parity gate added: `test_breadth_via_reduce_identical` (reduce-routed == single-process cell-for-cell) + `test_breadth_market_scalar_is_single_valued_per_minute` (one scalar/minute, the inverse of the 8-values symptom). Full suite 300 pass. **NEXT:** feature-value change ⇒ ships on the batched clean restart (SEQUENCING RULE), then **delete & recollect** all `source=stream` breadth. (NOT the audit loop's lane — engine/capture.)
- [ ] **HIGH — `sector_map` table is EMPTY (0 rows) → `sector` group 100% `unknown` + `breadth.sector_breadth_*` collapse to market breadth.** `db/init/06_sector_map.sql` creates the table but nothing populates it; there is no FMP `/profile` ingestion client anywhere in the repo (the `FMP_API_KEY` task was never wired). `loaders.py:47-52` LEFT JOINs the empty `sector_map`, so every symbol's sector is NULL → `sector.py:75` sends all names to `sector_is_unknown=1` (AAPL/JPM/XOM all `unknown`, verified). The group CODE is correct (anticipated in its docstring); this is a data-source/ingestion gap, NOT a feature fix. No bar recollection needed — sector one-hots re-derive the moment `sector_map` populates. FIX (ingest lane): wire FMP `/profile` → `sector_map` + the API key. Blocks 2 feature groups (sector + breadth's sector tier).
- [ ] **MED — `market_beta_*` grossly out-of-range (±7700 vs declared (-15,15)) from degenerate 2-point OLS fits.** The shared OLS guard `declarative.py:93` `defined = (b>=2) & (denom_x>0)` admits a slope from as few as 2 paired points with no min-periods floor and no `denom_x` epsilon, so near-flat-SPY windows on thin/warrant names explode (GLIX beta_120m=22350, corr=±1.0 = the 2-point signature; 6.7% of w=10 rows out of range). Same kernel live+backfill ⇒ parity-true but wrong-valued; pollutes any cross-sectional standardization. FIX (engine/modeller lane): require `b >= max(3, w//2)` and/or a relative `denom_x` floor in `_ols_stat_exprs`/`_ols_stat_numpy` (BOTH twins, to keep parity); null is the honest answer. Related: warrants/`.PR`/`.WS`/`.U`/leveraged-ETFs are in the feature-input universe across MANY groups (round_levels, market_beta, clean_momentum, breadth) — the 2026-06-11 ETF-pollution lesson recurring; needs a point-in-time single-name-equity universe pin on the STREAM feature path (distinct from the bars subscription).
- [ ] **HIGH (NEW, wave-4) — the daily-broadcast feature family is 100% NULL from the open until ~10:58 ET every session.** Three groups that read the settled `daily` frame — `multi_day_returns`, `multi_day_vwap`, `prior_day` — emit ALL their daily features as null from premarket through the first ~88 minutes of RTH, then flip to ~99.7% valued at the IDENTICAL 10:58 ET boundary (first valued minute written 15:00 UTC). Verified on fully-warmed names (AAPL has 240+ days of history yet `daily_return_1d`=null until 11:05 ET). This is NOT lookback warmup — it's the `daily` frame / daily cache (`consolidated.py` `_merged_daily` left-join) not being populated into the feature-computer until mid-morning, so it left-joins a not-yet-loaded frame → null broadcast. The shared 10:58 boundary across all three groups proves the cause is the daily SOURCE, not any group's logic. **Impact (money-relevant):** every multi-day momentum/mean-reversion/VWAP-distance signal is dark during the open — the most tradeable window — and it's invisible to tail-sampled audits (the prior multi_day_vwap audit sampled `fs[-60:]`, late session only, and reported "0.4% NaN", missing the entire early block). FIX (prod/ingest lane, owns daily-bar provisioning): load the settled daily frame at session start BEFORE the first emit; add a build/emit-time assert that the daily frame is non-empty for warmed names before broadcasting (turn this into a fail-loud check: "daily-broadcast groups are valued for warmed names within N min of session start"). Group code (multi_day.py, multi_day_vwap.py, prior_day.py) is correct — no per-group fix. Data is honest-null; the early window is unusable until load timing is fixed.
- [ ] **P0 (escalated, confirmed live across 4 groups, wave-4) — the STREAM feature universe is the contaminated 11,336-member / ~34-39%-fund set.** The feature-computer startup log prints `symbols=11336` for day=2026-06-15; `universe_membership WHERE in_universe` = 4,493/11,336 (39.6%) fund-like. Four universe-pinned groups now independently confirm leveraged/inverse ETFs are getting features computed and emitted: cross_sectional_rank (38.6% funds in the ranked set — TQQQ/SOXL/AAPB/AAPD/AAPU top the deciles, mechanically pushing every real equity's percentile down), multi_day_vwap (24.6%), multi_day_returns (27.6%), residual_analysis (SOXL/SQQQ/TQQQ/UPRO/SPXU/AAPD/AAPU present). This is the 2026-06-11 ETF-pollution lesson recurring at LIVE scale and WORSE (~39% vs the historical ~21% that polluted the "no edge" verdict). Same systemic as the QA loop's open P0 `etf-contamination` (`docs/QA_LEDGER.md`) and the MED note in the market_beta item above. FIX (prod/universe lane): re-apply the equities-only filter (`is_etf_like` in `select_universe`) to the universe rebuild; extend `universe_no_known_funds` / the denylist to broad-market ETFs (QQQ/SPY/IWM are NOT in `tests/fixtures/known_funds.txt`, so denylist-keyed checks UNDERCOUNT — name-regex via `asset_metadata` reads 38.6% where the denylist read ~33%). **Delete & recollect** all cross-sectional/universe-pinned stream-store partitions after re-clean. NOT a per-group code fix — every group inherits it via subscription.
- [ ] **MED (LOCAL-but-cross-lane, wave-4) — `cross_sectional_rank`'s per-minute universe pin is dead code; the rank set is "whoever printed," not a fixed membership.** `cross_sectional_rank.py:73,101` gate the universe pin on `ctx.frames["universe"]`, but `capture.py:263` never populates a universe snapshot into `frames`, so the branch never activates live; the ranked set = whatever symbols happened to print that minute (per-minute nsym swings 63→4,809). Even with a CLEAN universe table this is a live↔backfill parity hazard and noise source (a name present in backfill but absent live shifts everyone's percentile). FIX: capture supplies the per-day universe-membership frame into `ctx.frames["universe"]` for this group (the group code already consumes it). Group code ready; needs the capture wiring (cross-lane). Currently MASKED by the bigger universe-contamination P0.
- [ ] **Continue the group audit in waves of ~10** (read-only auditors → orchestrator dedups & fixes). DONE wave-1 (clean: distribution, candlestick, price_returns, price_volume; fixed/flagged: volume, technical, volatility, momentum, ohlc_vol, return_dynamics). REMAINING (~20): asset_flags, breadth, calendar, calendar_events, clean_momentum, cross_sectional_rank, efficiency, market_beta, market_context, momentum_consistency, momentum_run, multi_day_returns, multi_day_vwap, price_levels, prior_day, residual_analysis, round_levels, sector, swing, trend_quality. (efficiency + momentum_consistency already implicated in the points() bug above.) Record each group's verdict in the audit doc; fix duplicative roots once.

## P1 — Improvement (ordered; do the top unchecked one)
- [ ] **Stream trades + quotes** (currently bars-only: `real_capture` calls only `subscribe_bars`; `trade_flow`/`quote_spread`/`tick_runlength`/`microstructure_burst`/`liquidity` produce 0 data). Add `subscribe_trades`/`subscribe_quotes` + tick aggregation into those groups. Parity must hold. (Separate from the bar-feature work; enables 5 dormant groups.)
- [ ] **Per-symbol fast/tick path on the REAL feed** — the only route to sub-minute bet latency. Unify the real-feed entrypoint with the fast/tick path so a symbol's vector is emitted as its bar arrives, not after the minute closes. Measure before/after on the bar→vector Grafana dashboard. PARITY MUST HOLD (live == backfill).
  - **2026-06-15 increment (513ee8c / merge 59e3f77):** removed the fixed-symbol-set blocker — `IncrementalEngine` now handles a FLUCTUATING active set (live delivers ~4k of 11.3k symbols/minute, churning). Absent symbols fold a 0 contribution (== a missing bar in the batch) and are masked out of the OLS pairing; a genuinely-new ticker raises `SymbolSetExpanded` → `step()` re-seeds (parity-safe resync). Proven cell-for-cell vs `rust_windowed_sums` and vs batch `compute_latest` under churn (`tests/test_fp_incremental_membership.py`). Library-only, inert on live path, no deploy.
  - **DONE (`5479fd0`):** `IncrementalEngine` wired into `capture.process_bars` behind `FP_INCREMENTAL` (default OFF) for the batched `ReductionGroup`s — one engine per `reduce_input` bucket on `CaptureState` (`state.engines`), seeds lazily from the buffer on first `step` (re-seeds on `SymbolSetExpanded`), assembles via `step` (`assemble_from_long`, store-parity-true; the rust emit twins' warmup repr isn't gated yet so not used live). Runtime parity self-check `FP_INCREMENTAL_PARITY` computes BOTH each minute, writes the batch (truth), records the divergence-vs-tolerance ratio to Prometheus (`record_incremental_parity`). Default OFF = byte-identical to batch, no deploy risk.
  - **DONE — OPEN PARITY CONSTRAINT RESOLVED (`806d624` / merge `2eb033c`, `feat/slice-derive-sparse-parity`):** the `DERIVE_SLICE=6`-min window assumed active symbols are dense; a present symbol whose prior bar was >6 min back slice-derived a wrong null lag (backfill's `shift(k).over("symbol")` is POSITIONAL — the k-th prior ROW, however far back). **Fix:** `_matrix_at` now tails each present symbol's last `max_lag+1` ROWS (minute-sorted `group_by("symbol").tail`) — positionally exact, so the slice is cell-for-cell identical to the whole-buffer derive for dense AND sparse symbols; the derive still runs on ~`max_lag+1` rows/symbol (latency win preserved). Parity-gated: a sparse-symbol stream steps slice-vs-whole-buffer engines and asserts feature agreement every minute; verified the guard BITES under the old slice. 58 incremental+parity tests green. `FP_INCREMENTAL_SLICE` is now parity-safe (still default OFF pending live A/B).
  - **NEXT STEP (next cycle):** (a) **live A/B** — flip `FP_INCREMENTAL_PARITY=1` (and later `FP_INCREMENTAL_SLICE=1`) at the owner-supervised clean restart, watch the `record_incremental_parity` metric run clean, THEN flip `FP_INCREMENTAL=1` as the source of truth. (b) **zero-scan fold** — maintain the per-symbol last-`max_lag+1`-rows tail as engine STATE (O(active symbols)/min, no buffer materialize/sort), parity-gated like the row-tail; this is the final per-minute cost removal. (c) THEN **emit-on-arrival** (emit a symbol's vector as its bar arrives, not at minute close) — the actual sub-minute bet-latency win.
- [ ] **Parity validation ledger live** — run the after-market cycle so `feature_validation_day` / `feature_trust` populate; certify per-feature trust grades; gate training on certified features. (Crown jewel — empty today.)
- [ ] **Nightly re-seed + relaunch automation** — the capture is launched with a HARDCODED `2026-06-15` date arg; it will NOT roll to tomorrow. Wire a nightly job: re-seed universe for the new session + relaunch capture with the new date. Required for true autonomous daily operation.
- [ ] **Drop the global buffer-depth recompute tax** (follow-up from CRITICAL-2 buffer sizing, `e2a98c2`). `DEFAULT_BUFFER_MINUTES` is now 750 so swing's session-cumulative features match backfill, but that makes EVERY map group recompute over 750m each minute when only swing needs >300m (momentum_run needs 60m yet dominates the cost — ~8.3s/1250 at 720m). Two options: (a) **per-group buffer-depth slicing** — slice the materialized frame to each group's declared depth (max window + lag + slack) before `compute_latest`, parity-gated (the slice must keep all lag context); or (b) **stateful-swing accumulator** — swing's kernel is already an O(1)/bar state machine; retain its fold state across minutes (n_pivots_today, leg state, last-pivot minute per symbol) so it needs NO deep buffer, parity-gated like `WindowedSumState`. (b) is cleaner and removes the tax at the source. Neither blocks the clean restart.
- [ ] **Prometheus stale-rule cleanup** — `ingestor_alerts.yml` still references the retired `ingestor-coverage` job (now no series). Prune or repoint.
- [ ] **Latency drill-down hardening** — once the fast path lands, re-key the `bar_to_vector_latency` thresholds to the new architecture; confirm per-ticker drill-down still meaningful.

## P2 — Breadth & features (do after P1 is clear, or interleave when P1 is blocked)
- [ ] Port remaining high-value features from the old codebase (continue the FEATURE_PORT_PLAN).
- [ ] Real ADV-dollar ranking to replace the alphabetical/placeholder universe ordering once backfill history accrues.
- [ ] EDGAR real-time ingestion feature (filings event-clock).

## P3 — Research (non-blocking, when compute is idle)
- [ ] DL foundation-model prototyping on the clean panel (3090).

---

## How to REFILL (never idle)
If every P1/P2 item is checked or blocked, append new items drawn from: gaps the healthcheck WARNs at,
new failure modes seen in `docs/progress/`, feature ideas, parity edge cases, latency reductions, and
the Definition-of-Done criteria. Half the job is innovation — generate, don't stall.

## Log
- 2026-06-15: universe 3,070→11,336 (bias removed); dual latency metric + per-ticker drill-down + Grafana; continuous healthcheck engine + 5-min tripwire + MAINTENANCE_PROTOCOL.md. (integration/converged)
- 2026-06-15 ~11:40 ET: P1 #1 increment — `IncrementalEngine` fluctuating-symbol-set support (the live-integration blocker), parity-proven under membership churn. (513ee8c → merge 59e3f77, integration/converged)
- 2026-06-15 ~14:50 ET: P1 #1 increment — slice-derive OPEN PARITY CONSTRAINT RESOLVED. `_matrix_at` slices by per-symbol last-`max_lag+1`-rows tail (was a fixed 6-min window), positionally exact for sparse symbols ⇒ slice == whole-buffer cell-for-cell; `FP_INCREMENTAL_SLICE` now parity-safe. Sparse-stream parity gate added + verified to bite under the old slice; 58 incremental+parity tests green. Library-only, default OFF, no deploy. (`806d624` → merge `2eb033c`, integration/converged). Also recorded the already-landed `FP_INCREMENTAL` wiring (`5479fd0`). NEXT: live A/B via `FP_INCREMENTAL_PARITY`, then zero-scan fold (per-symbol tail state), then emit-on-arrival.
- 2026-06-15 ~14:30 ET: CRITICAL-3 breadth per-shard corruption FIXED-CODE — `breadth` added to `REDUCE_GROUPS` (whole-universe gather), full snapshots threaded through `process_reduce`, `reduce_buffer_minutes`=60; parity gate (reduce==single-process + one scalar/min). Full suite 300 pass. (`7f9a357` → merge to integration/converged). Ships on the clean restart; recollect stream breadth after.
- 2026-06-15 ~13:55 ET: CRITICAL-2 buffer-depth question RESOLVED (`DEFAULT_BUFFER_MINUTES` 300→750, measured: 410m insufficient, swing resets at UTC day, collection from premarket 08:00 UTC ⇒ ~720m needed; affordable vs 60s budget) + fixed 2 pre-existing trunk-red `test_fp_stream_sim` failures (resolve_points `__pt_*` not wired into the incremental emit path). Full suite 298 pass. (b0ce678, e2a98c2 → merge 9d479da, integration/converged). Follow-up queued: drop the global buffer recompute tax (per-group slice or stateful swing).
